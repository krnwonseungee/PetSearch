<!-- lines 1 - 8 changes background color on load. I found this was the most efficient
 way to use assign background colors for 2 different views.

 Wrapping this page around a div did not capture the whole page as with a body tag/complicated styling so it's not ideal, but it got the job done without having to unnecessarily create another layout. If there is a "cleaner" way to achieve this I'm open to suggestions.    -->

<script type="text/javascript">
  function changeBgColor(){
    $('body').css("background-color", "white")
  }
</script>

<body onload="changeBgColor()">

<div id="pet-name">
  <p><%= @pet.name %></p>
</div>

<%= link_to 'Back', pets_path, {id: 'back-link'} %>

<%= image_tag( "http://placehold.it/200X250" , :id => "profile-pic") %>


<div class="general-info-box">
  <div class="column">
    <strong class="title">
      Location
    </strong>
    <div class="holder">
      <%= @pet.contact_city %>, <%= @pet.contact_state %>
    </div>

    <strong class="title">
      Gender
    </strong>
    <div class="holder">
      <%= @pet.gender %>
    </div>
  </div>

  <div class="column-org-info">
    <strong class="title">
      Organization Contact
    </strong>
    <div class="holder">
      <%= @pet.organization_name %><br>
      <%= @pet.contact_street_address %><br>
      <%= @pet.contact_zipcode %><br>
      <%= @pet.contact_phone %><br>
      <%= @pet.contact_fax %><br>
      <%= @pet.contact_email %><br>
    </div>
  </div>
</div>

<div class="description-box">
  <h3>More About <%= @pet.name %></h3>
  <p><%= @pet.description %></p>
</div>

<table class="table-striped">
  <thead>
    <tr>
      <th> <%= @pet.primary_breed   %> - <%= @pet.secondary_breed   %></th>
    </tr>
  </thead>
  <tbody>
  <tr class="key-val-row"><td class="key">Age </td><td class="value"> <%= @pet.age  %></td></tr>
  <tr class="key-val-row"><td class="key">Size </td><td class="value"> <%= @pet.size %></td></tr>
  <tr class="key-val-row"><td class="key">Housebroken? </td><td class="value"> <%= @pet.is_housebroken %></td></tr>
  <tr class="key-val-row"><td class="key">Housetrained? </td><td class="value"> <%= @pet.is_housetrained %></td></tr>
  <tr class="key-val-row"><td class="key">Hypoallergenic? </td><td class="value"> <%= @pet.is_hypoallergenic %></td></tr>
  <tr class="key-val-row"><td class="key">Tail </td><td class="value"> <%= @pet.tail %></td></tr>
  <tr class="key-val-row"><td class="key">Color </td><td class="value"> <%= @pet.color %></td></tr>
  <tr class="key-val-row"><td class="key">Hair Length </td><td class="value"> <%= @pet.hair_length %></td></tr>
  <tr class="key-val-row"><td class="key">Contact Phone </td><td class="value"> <%= @pet.contact_phone %></td></tr>
  <tr class="key-val-row"><td class="key">Address </td><td class="value"><%= @pet.address %></td></tr>
  <tr class="key-val-row"><td class="key">Coordinates </td><td class="value"><%= @pet.coordinates %></td></tr>
  <tr class="key-val-row"><td class="key">Species </td><td class="value"><%= @pet.species %></td></tr>
  <tr class="key-val-row"><td class="key">Status </td><td class="value"><%= @pet.status ||= "--" %></td></tr>
